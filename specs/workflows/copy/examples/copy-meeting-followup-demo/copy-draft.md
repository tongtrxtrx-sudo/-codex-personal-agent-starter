# 文案初稿

## 会后同步标题
- AI workflow 脚本驱动推进会后同步

## 会后同步正文
今天下午的 AI 项目例会主要确认了 3 个方向：
1. 先把 `copy-workflow` 做成脚本驱动入口；
2. 图片 workflow 继续补示例和测试；
3. 下周再评估 `video workflow` 的脚本化优先级。

本次会议行动项如下：
- 张三：本周完成 `copy_workflow` CLI 验证；
- 李四：补图片 workflow 的第二个案例；
- 王敏：下周一前整理 `video workflow` 的接口草案。

请各负责人按时间点推进，如有阻塞及时同步。
