# 文案 Workflow 真实示例

这个目录演示如何用当前仓库里的文案 workflow 模板，跑完一次完整样例。

## 文件顺序
1. `raw-input.md`：原始需求
2. `structured-brief.json`：按模板整理后的结构化 brief
3. `copy-draft.md`：按输出模板生成的第一版文案
4. `review-result.md`：按审校清单给出的审校结果与修正版

## 示例目标
展示一条完整链路：

原始需求 -> Brief 抽取 -> 文案初稿 -> 审校修正 -> 人工确认
